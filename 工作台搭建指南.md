# OpenSourceDeck 快速开始与工作台搭建指南

> 基于本机实测（2026-09-17）整理。项目：`opensource-deck` v0.1.1，个人开源贡献工作台，把分散在 GitHub Issues / PRs / Reviews / CI 的贡献动态聚合为**只读、可解释**的中文仪表盘。
> 仓库已克隆在：`/data/usershare/project/TxBuddy/opensource-deck`

---

## 0. 三种使用形态

| 形态 | 适合 | 是否需要本地 Node | 是否需要改代码 |
|---|---|---|---|
| **A. GitHub Pages 静态部署** | 只想拥有自己的在线工作台 | 否 | 否（零配置 Fork） |
| **B. 本地只读 CLI（`osdeck`）** | 终端 / Agent 结构化分拣 | 是（≥24） | 否 |
| **C. 本地 Web 工作台（`npm run dev`）** | 本地看完整界面、改界面 | 是（≥24） | 否（默认用仓库内样例数据） |

---

## 1. 环境要求（本机已具备，勿新装 Node）

- **Node.js ≥ 24**（CLI 与本地开发都需要；仓库 `package.json` 声明 `engines.node >=24`）。
- 本机默认 `node` 是 **v20.18.1（太旧）**，可用的 ≥24 来源：
  - **conda `llm` 环境**：`/home/user/miniconda3/envs/llm/bin/node` → **v26.5.0**（npm 11.17.0）
  - **nvm**：`~/.nvm/versions/node/v24.15.0/bin/node` → **v24.15.0**
- 用法：构建/运行前把对应 bin 目录放到 PATH 最前：
  ```bash
  export PATH=/home/user/miniconda3/envs/llm/bin:$PATH   # 或 nvm 的 bin 目录
  node --version   # 应显示 v24+/v26+
  ```
- conda 根：`/home/user/miniconda3`（环境含 llm / ai-audit / ascope1x / cow_env / hermes_env / ua314）。

---

## 2. 方式 A：GitHub Pages（最快，零本地配置）

1. Fork 本仓库（[github.com/ranxi2001/opensource-deck](https://github.com/ranxi2001/opensource-deck/fork)）。
2. 进入 Fork 后的 **Actions** 页，按提示启用工作流。
3. **Settings → Pages**，Source 设为 **GitHub Actions**。
4. 回到 **Actions**，选 **Sync and deploy Pages**，点 **Run workflow** 首次运行。
5. 完成后打开 `https://<你的用户名>.github.io/opensource-deck/`（仓库名是 `<用户名>.github.io` 则直接 `https://<用户名>.github.io/`）。
- 工作流每小时自动刷新公开数据；组织账号下可在 Actions Variables 设 `OSDECK_GITHUB_USER`。
- **不会提交任何生成数据到你的仓库**，纯静态产物。

---

## 3. 方式 B：本地只读 CLI

```bash
export PATH=/home/user/miniconda3/envs/llm/bin:$PATH
cd /data/usershare/project/TxBuddy/opensource-deck
npm ci            # 安装依赖；prepare 脚本会自动 build:cli → dist-cli/osdeck.cjs
npm link          # 可选：注册全局 osdeck / opensource-deck 命令
osdeck summary --json
osdeck work --state needs_action --json
osdeck issues --signal contribution_label --json
osdeck show 'owner/repo#123' --json
osdeck url 'owner/repo#123'
```

- **数据源优先级**：`--source` / `OSDECK_SOURCE` → 本地公共缓存 → 官方部署快照 `https://onefly.top/opensource-deck/data/dashboard.json`（无 token 时回退到此）。
- **只读安全边界**（AGENTS.md 强制）：所有 GitHub 请求只读，CLI 只产出公开仓库数据；token 仅从 `GITHUB_TOKEN`/`GH_TOKEN` 读取，**绝不写入输出、绝不碰私有仓库**。
- 可选 `GITHUB_TOKEN`/`GH_TOKEN` 仅用于提升匿名 API 速率与增强公开数据。本地刷公共缓存：`osdeck sync --user <login>`。

---

## 4. 方式 C：本地 Web 工作台（dev）

```bash
export PATH=/home/user/miniconda3/envs/llm/bin:$PATH
cd /data/usershare/project/TxBuddy/opensource-deck
npm ci
npm run dev      # vite 开发服务器，默认用仓库内提交的公共样例数据
# 打开 http://localhost:5173
```

带**实时数据**（不提交）：
```bash
GITHUB_TOKEN="$(gh auth token)" npm run sync -- --output public/data/live.json
VITE_DATA_FILE=data/live.json npm run dev
```
> 严禁用具备私有仓库权限的 Token 生成静态 Pages 快照。

---

## 5. 生产构建 / 部署产物

```bash
npm run build    # = tsc -b && vite build，产出 dist/（Web 静态包）+ dist-cli/osdeck.cjs（CLI）
```

### ⚠️ 本机必看坑：CodeBuddy safe-delete 会卡死 vite build

本机 CodeBuddy 开了 **safe-delete**（`CODEBUDDY_SAFE_DELETE_ENABLED=1`）：`rm`/`rmSync` 被拦截改成"移到回收站 `/data/.Trash-1000`"，而该回收站目录**无写权限**，于是 vite/rolldown 在 `prepareOutDir` 清理旧 `dist` 时直接报：

```
[plugin vite:prepare-out-dir] Error: [safe-delete] 操作失败: ... /data/.Trash-1000 ... Permission denied
```

**解决**：构建前关闭 safe-delete（shell `rm` 与 node `fs.rm` 都会恢复成真删除）：

```bash
export CODEBUDDY_SAFE_DELETE_ENABLED=0
npm run build
```

（也可 `rm -rf dist` 后再构建，同样需先关该开关。）

---

## 6. Agent Skill（随仓库分发）

```bash
mkdir -p "${CODEX_HOME:-$HOME/.codex}/skills"
ln -s "$PWD/skills/opensource-deck" "${CODEX_HOME:-$HOME/.codex}/skills/opensource-deck"
```
之后在 Codex 中用 `$opensource-deck` 调用（指导 Agent 用结构化 CLI 输出、检查数据新鲜度、保守解释队列原因）。安装由用户显式执行，仓库不会改全局 Agent 设置。

---

## 7. 本机实测记录（2026-09-17）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 克隆 | `git clone https://gitee.com/buleboy8065/opensource-deck.git` | ✅ 落 `TxBuddy/opensource-deck` |
| 装依赖 | `npm ci`（conda llm node v26.5.0） | ✅ 279 包；`prepare` 自动 `build:cli` 成功 |
| CLI 冒烟 | `node dist-cli/osdeck.cjs summary --json` | ✅ 回退官方快照 `ranxi2001`：16 项目 / 92 事项，`syncStatus=success` |
| dev 工作台 | `npm run dev` | ✅ vite 起在 `:5173`（默认样例数据） |
| 生产构建 | `npm run build`（`CODEBUDDY_SAFE_DELETE_ENABLED=0`） | ✅ `dist/index.html` + `dist/assets/*`（1934 模块，820ms） |

---

## 8. 关键命令速查

| 目的 | 命令 |
|---|---|
| 装依赖（并自动构建 CLI） | `npm ci` |
| 起本地工作台 | `npm run dev` |
| 生产构建 | `CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run build` |
| CLI 总览 | `osdeck summary --json` |
| 待办队列 | `osdeck work --state needs_action --json` |
| 候选 Issue | `osdeck issues --signal contribution_label --json` |
| 单条详情 | `osdeck show 'owner/repo#123' --json` |
| 刷新公共缓存 | `osdeck sync --user <login>` |
| 全量校验 | `npm run check`（format+lint+typecheck+test+build+worker:check） |
| E2E | `npx playwright install --with-deps chromium && npm run test:e2e` |

> 原则（产品不变量）：只读、可解释（确定性原因码）、公共构建产物不入私有数据、私有 Token 仅存于中继加密 Cookie、工作台优先（打开即运营视图）。
